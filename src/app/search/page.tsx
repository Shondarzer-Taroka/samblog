import SearchPanel from '@/components/Search/SearchPanel';
import React from 'react';

const page = () => {
    return (
        <div>
            <SearchPanel/>
        </div>
    );
};

export default page;