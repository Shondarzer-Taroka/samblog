import Register from '@/components/Authentication/Register';
import React from 'react';

const page = () => {
    return (
        <div>
            <Register/>
        </div>
    );
};

export default page;