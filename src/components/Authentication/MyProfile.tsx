'use client'
import { useAuthProvider } from '@/Providers/AuthProvider';
import React from 'react';

const MyProfile = () => {
    useAuthProvider()
    return (
        <div>
            
        </div>
    );
};

export default MyProfile;